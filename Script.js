
    var word = "green bottles";
    var count = 10;

    while (count > 0) {
        console.log (count + " " + word + " hanging on the wall");
        console.log (count + " " + word + " hanging on the wall");
        console.log ("and if one green bottle should accidentally fall")
        count = count -1;

        if (count <= 0) {
            console.log ("There'll be no more" + word + "hanging on the wall");
        }
    }
     